services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: highland_games
      POSTGRES_USER: highland
      POSTGRES_PASSWORD: changeme
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: